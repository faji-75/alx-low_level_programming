#include <stdio.h>
/**
  * main - function rename it self
  * @argc: int type
  * @argv: char type
  * Return: return 0
  */
int main(int __attribute__((__unused__))argc, char *argv[])

{
	printf("%s\n", argv[0]);

	return (0);
}
