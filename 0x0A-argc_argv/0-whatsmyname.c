#include <stdio.h>
/**
  * main - function rename it self
  * @argc: int type
  * @argv: char type
  * Return: return 0
  */
int main(int argc, char *argv[])

{
if (argc > 0)
	printf("%s\n", argv[0]);

	return (0);
}
